# Mode State Machine

This repository has exactly two runtime modes:

- `normal`
- `redteam-active`

Red-team mode is **never** the default and must be explicitly armed by the user.

See:

- `/.agents/skills/red-team-command-doctrine/references/MODE-STATE-MACHINE.md`
